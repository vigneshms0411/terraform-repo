# Auto-generated by Terraform — do not edit manually
[webservers]
%{ 
  # vars passed as JSON strings; decode to lists
}
%{~ for i, ip in jsondecode(private_ips) ~}
web${i+1} ansible_host=${jsondecode(public_ips)[i]} ansible_ssh_host=${jsondecode(public_ips)[i]} private_ip=${ip}
%{~ endfor ~}

[all:vars]
ansible_user=${ansible_user}
# We use public IPs for SSH connectivity if Jenkins is outside the VPC.
# If Jenkins agent is inside VPC/VPN, you may replace ansible_host with the private IPs.